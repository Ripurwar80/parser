execution:
  - executor: jmeter
    scenario: blazedemo
    concurrency:
      local: 30
    hold-for: 10s
    ramp-up: 1s
reporting:
  - module: final-stats
    summary: true
    dump-csv: stats1.csv
